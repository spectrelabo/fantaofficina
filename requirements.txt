# Spectre - FantaMoneyball — Python Dependencies
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
requests>=2.28
beautifulsoup4>=4.12
scikit-learn>=1.3.0
scipy>=1.11.0
flask>=3.0.0

# fanta_lab_engine (opzionale, proprietario): se non installato, la pipeline
# usa automaticamente valori demo/placeholder. Vedi docs/superpowers/specs/2026-09-08-engine-split-design.md
# Installazione: pip install <ENGINE_WHEEL_URL> (secret CI/Vercel, non committare mai l'URL qui)
